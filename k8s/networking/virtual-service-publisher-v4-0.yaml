apiVersion: networking.istio.io/v1alpha3
kind: VirtualService
metadata:
  name: publisher
spec:
  hosts:
  - publisher
  http:
  - route:
    - destination:
        host: publisher
        subset: v4-0
