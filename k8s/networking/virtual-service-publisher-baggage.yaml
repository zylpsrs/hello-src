apiVersion: networking.istio.io/v1alpha3
kind: VirtualService
metadata:
  name: publisher
spec:
  hosts:
  - publisher
  http:
  - match:
    - headers:
        baggage-user-agent:
          exact: jason
    route:
    - destination:
        host: publisher
        subset: v3-2
  - route:
    - destination:
        host: publisher
        subset: v3-1
