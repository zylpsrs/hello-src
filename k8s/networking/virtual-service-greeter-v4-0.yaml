apiVersion: networking.istio.io/v1alpha3
kind: VirtualService
metadata:
  name: greeter-grpc
spec:
  hosts:
  - greeter-grpc
  http:
  - route:
    - destination:
        host: greeter-grpc
        subset: v4-0
