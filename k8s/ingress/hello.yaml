apiVersion: extensions/v1beta1
kind: Ingress
metadata:
  labels:
    app: hello
  name: hello
spec:
  rules:
  - host: hello-hello.apps.ocp.zyl.io
    http:
      paths:
      - backend:
          serviceName: hello
          servicePort: 9080
